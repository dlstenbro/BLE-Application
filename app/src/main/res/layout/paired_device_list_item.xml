<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/device_name"
        android:textSize="24sp"
        android:layout_alignStart="@+id/device_mac"
        android:layout_alignEnd="@+id/send_button" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/device_mac"
        android:layout_below="@+id/device_name"
        android:textSize="24sp"
        android:layout_alignStart="@+id/unpair_button"
        android:layout_alignEnd="@+id/send_button" />

    <Button
        android:text="Unpair"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/unpair_button"
        android:layout_below="@+id/device_mac"
        android:layout_alignParentStart="true"
        android:layout_marginStart="15dp"

        />

    <Button
        android:text="Send and Receive"
        android:layout_height="wrap_content"
        android:id="@+id/send_button"
        android:layout_width="wrap_content"
        android:layout_below="@+id/device_mac"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="14dp"

        />

</RelativeLayout>